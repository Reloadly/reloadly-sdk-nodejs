{
  "name": "@reloadly/reloadly.authentication",
  "version": "1.0.3",
  "description": "reloadly.authentication",
  "author": "reloadly",
  "devDependencies": {
    "@types/mocha": "^8.2.2",
    "@types/node": "^15.6.2",
    "mocha": "^8.4.0",
    "nyc": "^15.1.0",
    "typescript": "^4.2.4"
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "nyc mocha"
  },
  "dependencies": {
    "@reloadly/reloadly.core": "^1.0.0",
    "dotenv": "^10.0.0"
  },
  "main": "Authentication.js",
  "license": "MIT",
  "directories": {
    "test": "test"
  }
}
